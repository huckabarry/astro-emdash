diff --git a/node_modules/@emdash-cms/plugin-forms/src/admin.tsx b/node_modules/@emdash-cms/plugin-forms/src/admin.tsx
index 9cc282b..972ca03 100644
--- a/node_modules/@emdash-cms/plugin-forms/src/admin.tsx
+++ b/node_modules/@emdash-cms/plugin-forms/src/admin.tsx
@@ -611,6 +611,47 @@ function FormEditor({
 						value={settings.submitLabel ?? "Submit"}
 						onChange={(e) => setSettings((s) => ({ ...s, submitLabel: e.target.value }))}
 					/>
+					<div className="col-span-2">
+						<Input
+							label="Notification Emails"
+							value={(settings.notifyEmails ?? []).join(", ")}
+							onChange={(e) =>
+								setSettings((s) => ({
+									...s,
+									notifyEmails: e.target.value
+										.split(",")
+										.map((email) => email.trim())
+										.filter(Boolean),
+								}))
+							}
+							placeholder="you@example.com, another@example.com"
+						/>
+					</div>
+					<div className="col-span-2 flex flex-wrap items-end gap-4">
+						<Checkbox
+							label="Send daily digest instead of per-submission emails"
+							checked={Boolean(settings.digestEnabled)}
+							onCheckedChange={(checked) =>
+								setSettings((s) => ({
+									...s,
+									digestEnabled: checked,
+								}))
+							}
+						/>
+						{settings.digestEnabled && (
+							<Input
+								label="Digest Hour (0-23)"
+								type="number"
+								value={String(settings.digestHour ?? 9)}
+								onChange={(e) =>
+									setSettings((s) => ({
+										...s,
+										digestHour: Math.max(0, Math.min(23, parseInt(e.target.value, 10) || 9)),
+									}))
+								}
+							/>
+						)}
+					</div>
 					<div>
 						<Select
 							label="Spam Protection"
@@ -1280,6 +1321,7 @@ function RecentSubmissionsWidget() {
 
 export const pages: PluginAdminExports["pages"] = {
 	"/": FormsListPage,
+	"/settings": FormsListPage,
 	"/submissions": SubmissionsPage,
 };
 
