---
import Base from "../../layouts/Base.astro";
import { getPopfeedItemsByType } from "../../lib/media";

const items = await getPopfeedItemsByType("book");
---

<Base title="Books" description="Books from the media log.">
	<section class="stream-head">
		<h1 class="stream-head__title">Books</h1>
		<p class="stream-head__lede">Books from the media log.</p>
	</section>

	<section class="blog-list blog-list--archive" aria-label="Books">
		{items.map((item) => (
			<article class="blog-row">
				<a class="blog-row__link" href={item.localPath}>
					<time class="blog-row__date" datetime={item.date.toISOString()}>{item.displayDate}</time>
					<h2>{item.title}</h2>
					<p>{item.mainCredit || item.listTypeLabel}</p>
					<span class="blog-row__read-more">Open book <span aria-hidden="true">→</span></span>
				</a>
			</article>
		))}
	</section>
</Base>
